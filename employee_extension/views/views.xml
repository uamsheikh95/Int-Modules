<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="employee_extension.list">
      <field name="name">employee_extension list</field>
      <field name="model">employee_extension.employee_extension</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="employee_extension.action_window">
      <field name="name">employee_extension window</field>
      <field name="res_model">employee_extension.employee_extension</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="employee_extension.action_server">
      <field name="name">employee_extension server</field>
      <field name="model_id" ref="model_employee_extension_employee_extension"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="employee_extension" id="employee_extension.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="employee_extension.menu_1" parent="employee_extension.menu_root"/>
    <menuitem name="Menu 2" id="employee_extension.menu_2" parent="employee_extension.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="employee_extension.menu_1_list" parent="employee_extension.menu_1"
              action="employee_extension.action_window"/>
    <menuitem name="Server to list" id="employee_extension" parent="employee_extension.menu_2"
              action="employee_extension.action_server"/>
    -->
  </data>
</odoo>