<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="exam_bank.list">
      <field name="name">exam_bank list</field>
      <field name="model">exam_bank.exam_bank</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="exam_bank.action_window">
      <field name="name">exam_bank window</field>
      <field name="res_model">exam_bank.exam_bank</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="exam_bank.action_server">
      <field name="name">exam_bank server</field>
      <field name="model_id" ref="model_exam_bank_exam_bank"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="exam_bank" id="exam_bank.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="exam_bank.menu_1" parent="exam_bank.menu_root"/>
    <menuitem name="Menu 2" id="exam_bank.menu_2" parent="exam_bank.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="exam_bank.menu_1_list" parent="exam_bank.menu_1"
              action="exam_bank.action_window"/>
    <menuitem name="Server to list" id="exam_bank" parent="exam_bank.menu_2"
              action="exam_bank.action_server"/>
    -->
  </data>
</odoo>