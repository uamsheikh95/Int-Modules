<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="amaana.list">
      <field name="name">amaana list</field>
      <field name="model">amaana.amaana</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="amaana.action_window">
      <field name="name">amaana window</field>
      <field name="res_model">amaana.amaana</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="amaana.action_server">
      <field name="name">amaana server</field>
      <field name="model_id" ref="model_amaana_amaana"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="amaana" id="amaana.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="amaana.menu_1" parent="amaana.menu_root"/>
    <menuitem name="Menu 2" id="amaana.menu_2" parent="amaana.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="amaana.menu_1_list" parent="amaana.menu_1"
              action="amaana.action_window"/>
    <menuitem name="Server to list" id="amaana" parent="amaana.menu_2"
              action="amaana.action_server"/>
    -->
  </data>
</odoo>