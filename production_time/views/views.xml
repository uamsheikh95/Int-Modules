<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="production_time.list">
      <field name="name">production_time list</field>
      <field name="model">production_time.production_time</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="production_time.action_window">
      <field name="name">production_time window</field>
      <field name="res_model">production_time.production_time</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="production_time.action_server">
      <field name="name">production_time server</field>
      <field name="model_id" ref="model_production_time_production_time"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="production_time" id="production_time.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="production_time.menu_1" parent="production_time.menu_root"/>
    <menuitem name="Menu 2" id="production_time.menu_2" parent="production_time.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="production_time.menu_1_list" parent="production_time.menu_1"
              action="production_time.action_window"/>
    <menuitem name="Server to list" id="production_time" parent="production_time.menu_2"
              action="production_time.action_server"/>
    -->
  </data>
</odoo>