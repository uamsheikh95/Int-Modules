<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="project_invoice_bill.list">
      <field name="name">project_invoice_bill list</field>
      <field name="model">project_invoice_bill.project_invoice_bill</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="project_invoice_bill.action_window">
      <field name="name">project_invoice_bill window</field>
      <field name="res_model">project_invoice_bill.project_invoice_bill</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="project_invoice_bill.action_server">
      <field name="name">project_invoice_bill server</field>
      <field name="model_id" ref="model_project_invoice_bill_project_invoice_bill"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="project_invoice_bill" id="project_invoice_bill.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="project_invoice_bill.menu_1" parent="project_invoice_bill.menu_root"/>
    <menuitem name="Menu 2" id="project_invoice_bill.menu_2" parent="project_invoice_bill.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="project_invoice_bill.menu_1_list" parent="project_invoice_bill.menu_1"
              action="project_invoice_bill.action_window"/>
    <menuitem name="Server to list" id="project_invoice_bill" parent="project_invoice_bill.menu_2"
              action="project_invoice_bill.action_server"/>
    -->
  </data>
</odoo>