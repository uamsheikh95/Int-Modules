<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="payment_to_account.list">
      <field name="name">payment_to_account list</field>
      <field name="model">payment_to_account.payment_to_account</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="payment_to_account.action_window">
      <field name="name">payment_to_account window</field>
      <field name="res_model">payment_to_account.payment_to_account</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="payment_to_account.action_server">
      <field name="name">payment_to_account server</field>
      <field name="model_id" ref="model_payment_to_account_payment_to_account"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="payment_to_account" id="payment_to_account.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="payment_to_account.menu_1" parent="payment_to_account.menu_root"/>
    <menuitem name="Menu 2" id="payment_to_account.menu_2" parent="payment_to_account.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="payment_to_account.menu_1_list" parent="payment_to_account.menu_1"
              action="payment_to_account.action_window"/>
    <menuitem name="Server to list" id="payment_to_account" parent="payment_to_account.menu_2"
              action="payment_to_account.action_server"/>
    -->
  </data>
</odoo>